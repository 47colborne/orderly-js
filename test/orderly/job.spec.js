import { assert, expect, lib, sinon } from '../test_helper'

let Job = lib.src('orderly/job').default

describe('Job', function() {

  describe('constructor', function() {
    context('given a priority', function() {
      it('sets priority', function() {
        expect()
      })
    })
    context('given no priority', function() {
      it('should set default priority to 0', function() {

      })
    })

  })


})
